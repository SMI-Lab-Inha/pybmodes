# IEA-22-280-RWT on the IEA Wind Task 55 semi-submersible

IEA-22 semi-submersible floating substructure: tower modelled as a free-free flexible beam (hub_conn = 2) with a PlatformSupport block assembled programmatically from the upstream IEA-22 OpenFAST decks (HydroDyn_PotMod variant for WAMIT data).

## Files

| File                                       | Purpose                              |
| ------------------------------------------ | ------------------------------------ |
| `10_iea22_semi_tower.bmi`                   | Tower BMI (floating with PlatformSupport)         |
| `10_iea22_semi_tower_sec_props.dat`         | Distributed tower section data       |
| `10_iea22_semi_blade.bmi`                   | Blade BMI (rotating cantilever)      |
| `10_iea22_semi_blade_sec_props.dat`         | Distributed blade section data       |

## How to run

```python
from pybmodes.models import Tower, RotatingBlade

tower = Tower("src/pybmodes/_examples/sample_inputs/reference_turbines/10_iea22_semi/10_iea22_semi_tower.bmi")
tower_modal = tower.run(n_modes=8)
print("tower freqs (Hz):", tower_modal.frequencies[:4])

blade = RotatingBlade("src/pybmodes/_examples/sample_inputs/reference_turbines/10_iea22_semi/10_iea22_semi_blade.bmi")
blade_modal = blade.run(n_modes=8)
print("blade freqs (Hz):", blade_modal.frequencies[:4])
```

## Tower BMI structure

This sample is a free-free flexible-tower (`hub_conn = 2`) with a full PlatformSupport block carrying the platform 6 × 6 hydro / mooring / inertia matrices.

## pyBmodes frequencies (this BMI, deck-as-distributed)

### Tower

- 1st FA tower-bending: **0.3369 Hz**

### Blade  (spinning at deck `RotSpeed = 3 rpm`)

- 1st flap: **0.3998 Hz**
- 1st edge: **0.5381 Hz**
- 2nd flap: **1.1032 Hz**

## Comparison with published values

The original publication for this RWT printed a 1st-FA tower-bending frequency of **~ 0.3400 Hz** (IEA-22-280-RWT OpenFAST regression on the as-distributed Semi configuration; no peer-reviewed publication ships with v1.0 (technical report still in preparation per the IEA-22-280-RWT repository README)). Reference-wind-turbine structural definitions are **iteratively revised** across releases — the same RWT designation at git-tag v1.0.0 may have a few-percent different section-property distribution than at v2.0.0. The pyBmodes frequency above is derived from the deck-as-distributed at the time this sample was last built, so it need not match the publication's printed value exactly. A drift between them usually reflects deck-revision evolution, not a pyBmodes error — treat the published value as a historical anchor, not a regression target.

## Citation

Bortolotti, P., et al. (2024). IEA Wind TCP Task 55: IEA Wind 22 MW Reference Wind Turbine. NREL technical report (in preparation); repository: IEAWindSystems/IEA-22-280-RWT.

## Provenance

All numerical values in these BMI and section-properties files are sourced from the named publication's distributed structural data. The file format and commentary are pyBmodes-authored (MIT-licensed). Generated by `src/pybmodes/_examples/sample_inputs/reference_turbines/build.py`.
