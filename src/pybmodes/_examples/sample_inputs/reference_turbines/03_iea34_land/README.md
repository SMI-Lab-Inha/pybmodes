# IEA-3.4-130-RWT — land-based

Land-based (cantilever clamped at ground level).

## Files

| File                                       | Purpose                              |
| ------------------------------------------ | ------------------------------------ |
| `03_iea34_land_tower.bmi`                   | Tower BMI (cantilever)         |
| `03_iea34_land_tower_sec_props.dat`         | Distributed tower section data       |
| `03_iea34_land_blade.bmi`                   | Blade BMI (rotating cantilever)      |
| `03_iea34_land_blade_sec_props.dat`         | Distributed blade section data       |

## How to run

```python
from pybmodes.models import Tower, RotatingBlade

tower = Tower("src/pybmodes/_examples/sample_inputs/reference_turbines/03_iea34_land/03_iea34_land_tower.bmi")
tower_modal = tower.run(n_modes=8)
print("tower freqs (Hz):", tower_modal.frequencies[:4])

blade = RotatingBlade("src/pybmodes/_examples/sample_inputs/reference_turbines/03_iea34_land/03_iea34_land_blade.bmi")
blade_modal = blade.run(n_modes=8)
print("blade freqs (Hz):", blade_modal.frequencies[:4])
```

## Tower BMI structure

This sample is a cantilever (`hub_conn = 1`) clamped at TowerBsHt with the RNA lumped at the tower top.

## pyBmodes frequencies (this BMI, deck-as-distributed)

### Tower

- 1st FA tower-bending: **0.4172 Hz**

### Blade  (spinning at deck `RotSpeed = 5 rpm`)

- 1st flap: **0.7958 Hz**
- 1st edge: **1.0294 Hz**
- 2nd flap: **2.3593 Hz**

## Comparison with published values

The original publication for this RWT printed a 1st-FA tower-bending frequency of **~ 0.4200 Hz** (Bortolotti et al. 2019 §3.4, Table 3-12 (IEA-3.4-130-RWT 1st-FA tower-bending)). Reference-wind-turbine structural definitions are **iteratively revised** across releases — the same RWT designation at git-tag v1.0.0 may have a few-percent different section-property distribution than at v2.0.0. The pyBmodes frequency above is derived from the deck-as-distributed at the time this sample was last built, so it need not match the publication's printed value exactly. A drift between them usually reflects deck-revision evolution, not a pyBmodes error — treat the published value as a historical anchor, not a regression target.

## Citation

Bortolotti, P., Tarrés, H. C., Dykes, K., Merz, K., Sethuraman, L., Verelst, D., & Zahle, F. (2019). IEA Wind TCP Task 37: Systems Engineering in Wind Energy — WP2.1 Reference Wind Turbines. NREL/TP-5000-73492.

## Provenance

All numerical values in these BMI and section-properties files are sourced from the named publication's distributed structural data. The file format and commentary are pyBmodes-authored (MIT-licensed). Generated by `src/pybmodes/_examples/sample_inputs/reference_turbines/build.py`.
