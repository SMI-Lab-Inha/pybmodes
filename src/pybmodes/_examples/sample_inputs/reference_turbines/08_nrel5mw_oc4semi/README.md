# NREL 5MW on the OC4 DeepCwind floating semi-submersible

OC4 DeepCwind floating semi-submersible: tower modelled as a free-free flexible beam (hub_conn = 2) with a PlatformSupport block assembled programmatically from the upstream r-test 5MW_OC4Semi_WSt_WavesWN ElastoDyn + HydroDyn + MoorDyn decks.

## Files

| File                                       | Purpose                              |
| ------------------------------------------ | ------------------------------------ |
| `08_nrel5mw_oc4semi_tower.bmi`                   | Tower BMI (floating with PlatformSupport)         |
| `08_nrel5mw_oc4semi_tower_sec_props.dat`         | Distributed tower section data       |
| `08_nrel5mw_oc4semi_blade.bmi`                   | Blade BMI (rotating cantilever)      |
| `08_nrel5mw_oc4semi_blade_sec_props.dat`         | Distributed blade section data       |

## How to run

```python
from pybmodes.models import Tower, RotatingBlade

tower = Tower("src/pybmodes/_examples/sample_inputs/reference_turbines/08_nrel5mw_oc4semi/08_nrel5mw_oc4semi_tower.bmi")
tower_modal = tower.run(n_modes=8)
print("tower freqs (Hz):", tower_modal.frequencies[:4])

blade = RotatingBlade("src/pybmodes/_examples/sample_inputs/reference_turbines/08_nrel5mw_oc4semi/08_nrel5mw_oc4semi_blade.bmi")
blade_modal = blade.run(n_modes=8)
print("blade freqs (Hz):", blade_modal.frequencies[:4])
```

## Tower BMI structure

This sample is a free-free flexible-tower (`hub_conn = 2`) with a full PlatformSupport block carrying the platform 6 × 6 hydro / mooring / inertia matrices.

## pyBmodes frequencies (this BMI, deck-as-distributed)

### Tower

- 1st FA tower-bending: **0.4507 Hz**

### Blade  (spinning at deck `RotSpeed = 9 rpm`)

- 1st flap: **0.7118 Hz**
- 1st edge: **1.1043 Hz**
- 2nd flap: **1.9913 Hz**

## Comparison with published values

The original publication for this RWT printed a 1st-FA tower-bending frequency of **~ 0.4400 Hz** (Robertson et al. 2014 §3.5: 1st-FA tower-bending of the OC4 DeepCwind semi system). Reference-wind-turbine structural definitions are **iteratively revised** across releases — the same RWT designation at git-tag v1.0.0 may have a few-percent different section-property distribution than at v2.0.0. The pyBmodes frequency above is derived from the deck-as-distributed at the time this sample was last built, so it need not match the publication's printed value exactly. A drift between them usually reflects deck-revision evolution, not a pyBmodes error — treat the published value as a historical anchor, not a regression target.

## Citation

Robertson, A. N., Jonkman, J. M., Masciola, M., Song, H., Goupee, A. J., Coulling, A., & Luan, C. (2014). Definition of the Semisubmersible Floating System for Phase II of OC4. NREL/TP-5000-60601.

## Provenance

All numerical values in these BMI and section-properties files are sourced from the named publication's distributed structural data. The file format and commentary are pyBmodes-authored (MIT-licensed). Generated by `src/pybmodes/_examples/sample_inputs/reference_turbines/build.py`.
